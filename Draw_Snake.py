import pygame
import time

class Snake:
	def __init__(surface, colour, position, radius, width = 0):
		"""Creates the properties that the snake is going to have throughout the game"""
		for snake_body in range(10):
			pass
		pygame.draw.circle()
		

